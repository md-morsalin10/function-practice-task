function getAge(person){
    const age = person.age;
    return age;
}

const findage  =  getAge(person={
                    name: "Morsalin",
                    age:24
                     })

                     console.log(findage)